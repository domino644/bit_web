import languages from "./languages";

interface laureate {
  id: number;
  knownName: languages;
  fullName: languages;
  portion: string;
  sortOrder: string;
  motivation: languages;
}

export default laureate;
